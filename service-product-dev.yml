global:
  message: Estás en el entorno DEV
spring:
  application:
    name: service-product-dev
  datasource:
    url: jdbc:postgresql://localhost:5432/productos_db
    username: f5antos
    password: 123456
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081
